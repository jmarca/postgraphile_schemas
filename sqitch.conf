[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "postgraphile"]
	# uri = db:pg://postgres@127.0.0.1:6543/forum_test
[engine "pg"]
        # target = postgraphile
[deploy]
	verify = true
[rebase]
	verify = true
[core "variables"]
        # change these to suit your setup
	DATABASE_OWNER = slashcode
	DATABASE_OWNER_PASSWORD = graceemma
	DATABASE_AUTHENTICATOR = authenticated
	DATABASE_VISITOR = anonymous
